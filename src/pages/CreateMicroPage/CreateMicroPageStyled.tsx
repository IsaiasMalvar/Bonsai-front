import styled from "styled-components";

const CreateMicroPageStyled = styled.main`
  .create-page {
    &__title {
      margin-top: 25px;
      font-size: 24px;
      font-weight: bold;
    }
  }
`;

export default CreateMicroPageStyled;
